"""Configurable U-Net (DynUNet) nodes for cuvis-ai.

Node classes are discovered by the ``plugins.yaml`` manifest, not by import-time
registration, so this package root intentionally exposes no side effects.
"""
